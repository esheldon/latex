
texfile := 2016-04-12-group-meeting

default: pdf


pdf:
	pdflatex ${texfile}

clean:
	rm -f \
	${texfile}.dvi \
	${texfile}.ps \
	${texfile}.nav \
	${texfile}.out \
	${texfile}.snm \
	${texfile}.pdf \
	${texfile}.aux \
	${texfile}.bbl \
	${texfile}.blg \
	${texfile}.toc \
	${texfile}.log
